spring:
  application:
    name: service-organization

  profiles:
    active: new

  cloud:
    config:
      uri: http://localhost:8888

encrypt:
  key-store:
    location: classpath:server.jks
    alias: localkey
    password: cDK37yBbM