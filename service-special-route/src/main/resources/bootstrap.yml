spring:
  application:
    name: service-special-route

  profiles:
    active: local

  cloud:
    config:
      uri: http://localhost:8888

encrypt:
  key-store:
    location: classpath:server.jks
    alias: localkey
    password: cDK37yBbM

