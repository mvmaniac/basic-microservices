pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'basic-microservices'

include 'server-eureka', 'server-gateway', 'server-config', 'service-common', 'service-member', 'service-catalog', 'service-order'
